[anim, pred, tflt, object, motion] = smg.next();
seqorg = DataPackage.tcat(anim, pred);
flted = DataPackage(MathLib.nnconv(anim.data, tflt.data, zeros(1,3), 'same'), 2, true);
seqflted = DataPackage.tcat(anim, flted);
anim.tcombine();
predflt = aunit.forward(lunitA.forward(cunitA.forward(anim))).reshape(tfltsize);
preded = DataPackage(MathLib.nnconv(anim.data, predflt.data, zeros(1, 3), 'same'), 2, true);
predlabel = convop.forward(anim, aunit.forward(lunit.forward(cunit.forward(anim))).reshape(tfltsize));
predlabel.tsplit();
anim.tsplit();
seqpred = DataPackage.tcat(anim, preded);
seqlabel = DataPackage.tcat(anim, predlabel);
animcompare(seqorg, seqflted);
animcompare(seqpred, seqlabel);